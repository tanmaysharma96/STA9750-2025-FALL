---
title: "Tanmay Sharma - Data Science Portfolio"
format: html
---
<img src="tanmay.JPG" alt="Tanmay Sharma" 
     style="float:right; width:180px; margin:0 0 20px 20px; border-radius:15px;" />

# Welcome 👋

Hi, I’m **Tanmay Sharma**, a Master’s student at [Baruch College](https://zicklin.baruch.cuny.edu/).  
I am passionate about turning data into actionable insights. My work spans data analysis, visualization, and predictive modeling, helping uncover patterns and drive smarter decisions. I enjoy exploring real-world datasets, building interactive tools, and communicating findings in a clear, impactful way.

You can also connect with me on [LinkedIn](https://www.linkedin.com/in/tanmay-sharma-3146bb160).

P.S. Scroll down if you feel like testing your knowledge!

---

## Interactive Map

```{r}
#| echo: false
#| message: false
#| warning: false
#| fig.height: 6     # <-- add a height in inches
#| fig.width: 8

if(!require("leaflet")){
  options(repos=c(CRAN="https://cloud.r-project.org"))
  install.packages("leaflet")
  stopifnot(require("leaflet"))
}

baruch_longitude <- -73.98333
baruch_latitude  <- 40.75028

leaflet() |>
  addTiles() |>
  setView(baruch_longitude, baruch_latitude, zoom=17) |>
  addPopups(baruch_longitude, baruch_latitude, 
            "I am a Master's student at <b>Baruch College</b>!")
```

---

## Want to test your data science knowledge? Try this quiz!

---
<div style="margin:20px; padding:20px; border:2px solid #ccc; border-radius:10px; max-width:500px;">
  <h3>Data Science Quiz</h3>
  <p id="question"></p>
  <div id="options"></div>
  <button onclick="nextQuestion()">Next</button>
  <p id="feedback" style="font-weight:bold;"></p>
</div>

<script>
  const quiz = [
    {
      q: "Which of these is a supervised learning algorithm?",
      options: ["K-means", "Linear Regression", "PCA"],
      answer: "Linear Regression"
    },
    {
      q: "What does 'overfitting' mean?",
      options: [
        "Model performs well on training but poorly on test data",
        "Model generalizes perfectly",
        "Model has too little variance"
      ],
      answer: "Model performs well on training but poorly on test data"
    },
    {
      q: "Which library is commonly used for data visualization in R?",
      options: ["NumPy", "ggplot2", "scikit-learn"],
      answer: "ggplot2"
    }
  ];

  let current = 0;

  function loadQuestion() {
    document.getElementById("feedback").innerHTML = "";
    const q = quiz[current];
    document.getElementById("question").innerText = q.q;
    const optionsDiv = document.getElementById("options");
    optionsDiv.innerHTML = "";
    q.options.forEach(opt => {
      const btn = document.createElement("button");
      btn.innerText = opt;
      btn.style.margin = "5px";
      btn.onclick = () => {
        if(opt === q.answer){
          document.getElementById("feedback").innerHTML = "✅ Correct!";
        } else {
          document.getElementById("feedback").innerHTML = "❌ Wrong. Try again.";
        }
      };
      optionsDiv.appendChild(btn);
    });
  }

  function nextQuestion() {
    current = (current + 1) % quiz.length;
    loadQuestion();
  }

  // load first question on page load
  loadQuestion();
</script>
